/**
 * Top 10 #6 — Typografi-roller (minimal skala).
 * Brug som `className={\`${TYPE_BODY} …\`}` og tilføj farve/font-modifikatorer efter behov.
 */

/** Sektions-overline (uppercase, tracking) */
export const TYPE_OVERLINE = 'text-[10px] font-bold uppercase tracking-wider text-muted'

/** Overline med stærkere kontrast */
export const TYPE_OVERLINE_STRONG = 'text-[10px] font-bold uppercase tracking-wider text-muted-strong'

/** Meget fin brød / diagram-fod */
export const TYPE_CAPTION = 'text-[9px] text-muted'

/** Label under store tal (stat tiles, workout summary grid) */
export const TYPE_LABEL_UPPER = 'text-[9px] font-bold text-muted uppercase tracking-[0.5px]'

/** Sekundær linje under titel */
export const TYPE_META = 'text-[10px] text-muted'

/** Primær brød, lister, søgefelt-input */
export const TYPE_BODY = 'text-[13px] text-text'

export const TYPE_BODY_SEMIBOLD = 'text-[13px] font-semibold text-text'

/** Kompakt brød, chips, procent-badges */
export const TYPE_BODY_SM = 'text-[12px]'

export const TYPE_BODY_SM_SEMIBOLD = 'text-[12px] font-semibold text-text'

/** Kort- / række-titel */
export const TYPE_TITLE_ROW = 'text-[14px] font-bold text-text'

/** Kompakt tal-blok (fx “at that time”) */
export const TYPE_NUMBER_COMPACT = 'text-[14px] font-extrabold text-text'

/** Blok-titel (deep-dive, m.m.) */
export const TYPE_TITLE_BLOCK = 'text-[15px] font-extrabold text-text'

/** Modaldialog titel (semibold/bold 15px) */
export const TYPE_TITLE_MODAL = 'text-[15px] font-bold text-text'

/** Bottom sheet / modal titel */
export const TYPE_SHEET_TITLE = 'text-[17px] font-extrabold text-text'

/** Store tal (overview stat tiles) */
export const TYPE_DISPLAY = 'text-[20px] font-extrabold text-text leading-none'

/** Ekstra stort tal (fx recovery overall %) */
export const TYPE_DISPLAY_LG = 'text-[26px] font-extrabold'

/** 4-kolonne workout stats (detail sheet) */
export const TYPE_STAT_NUMBER = 'text-[18px] font-extrabold text-text'

/** Fremhævet tal i session-kort (fx sidste sæt) */
export const TYPE_STAT_EMPHASIS = 'text-[16px] font-extrabold text-text'

/** Enhed ved siden af display-tal */
export const TYPE_UNIT_SUFFIX = 'text-[10px] text-muted font-semibold'

/** Tabellignende meta, dato-kolonner */
export const TYPE_MICRO = 'text-[11px] text-muted'

/** Under-titel i sheet (accent) */
export const TYPE_SUBTITLE = 'text-[12px] font-semibold text-accent'

/** Lille fremhævet linje (delta, links) */
export const TYPE_EMPHASIS_SM = 'text-[10px] font-bold'

/** Ekstra lille label (recovery grid, foto) */
export const TYPE_MICRO_TIGHT = 'text-[8px] text-muted'

export const TYPE_RING_PCT = 'text-[9px] font-extrabold text-white'

/** Ekstra lille overskrift (recovery-grid muskelnavne) */
export const TYPE_LABEL_MICRO = 'text-[8px] font-bold text-muted uppercase tracking-[0.4px] text-center'

/** Formularfelt-labels (semibold) */
export const TYPE_LABEL_FORM = 'text-[10px] font-semibold text-muted uppercase tracking-wider'

/** Side-/paneloverskrift (fx Photos) */
export const TYPE_HEADING_PAGE = 'text-[18px] font-extrabold text-text'

/** Overline med muted-mid (workout complete m.m.) */
export const TYPE_OVERLINE_MID = 'text-[10px] font-bold text-muted-mid uppercase tracking-wider'

/** Segment / faner (Start | Plan, vinkler) */
export const TYPE_TAB = 'text-[11px] font-bold'

/** “IN PROGRESS” m.m. på start-kort */
export const TYPE_STATUS_BADGE = 'text-[10px] font-extrabold tracking-[0.8px]'

/** Stort “+” på programme tiles */
export const TYPE_HERO_PLUS = 'text-[22px] font-light'
