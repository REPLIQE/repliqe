<?xml version="1.0" encoding="utf-8"?>
<!--
  1x1 fully transparent vector. Wired into AppTheme.NoActionBarLaunch as
  windowSplashScreenAnimatedIcon so the Android 12+ system splash renders
  ONLY the dark window background. The brand splash (inline SVG in
  index.html) then paints the actual REPLIQE composition without the
  centered launcher icon "bumping" position during the handoff.
  Do not delete: AndroidX SplashScreen falls back to the launcher icon
  if windowSplashScreenAnimatedIcon is omitted.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="1dp"
    android:height="1dp"
    android:viewportWidth="1"
    android:viewportHeight="1">
    <path
        android:fillColor="#00000000"
        android:pathData="M0,0h1v1H0z" />
</vector>
